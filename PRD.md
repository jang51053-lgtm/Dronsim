# 드론 비행 시뮬레이션 — PRD (v0.1 초안)

## 1. 개요

- **컨셉**: 교실에서 태블릿(주)/PC(병행)로 플레이하는 드론 조종 시뮬레이션 게임. 드론과 장비를 고르고, 단계별 비행 과제를 수행하며 실력을 키운 뒤, 최종적으로 다른 드론과 겨루는 "드론 비행 격투" 모드로 마무리.
- **대상**: 초등학생, 학급 활동용
- **플랫폼/조작**: 태블릿 터치를 기본으로 설계하고, PC 키보드+마우스도 병행 지원
- **시점**: 3인칭 추격 시점 (드론 뒤에서 따라가는 카메라)
- **핵심 재미**: (1) 나만의 드론을 커스터마이징하는 수집/성장 재미 (2) 비행 과제를 클리어하는 조작 숙련 재미 (3) 마지막 격투 모드의 대결 재미

## 2. 핵심 게임 루프

```
드론 선택 → 장비 장착 → 비행 과제(미션) 수행 → 보상 획득(코인/부품)
   ↑                                                    ↓
   └──────────── 장비 업그레이드 / 새 드론 해금 ←────────┘
                         ↓ (일정 단계 이상 진행 시)
                    전투 모드 해금
```

## 3. 시스템별 기획

### 3.1 드론 선택
> **결정 (2026-09-13)**: 기체 라인업(레이서형/균형형/탱커형 등 다중 기체)은 채택하지 않음. **지금 쓰는 드론 모델 1종만 사용**한다. 기체 자체를 여러 종류로 나누는 대신, 아래 3.2 장비 슬롯(총기·카메라 등)으로 능력/기능을 확장하는 방식으로 단순화.

- ~~기체별 스탯 4종(속도/기동성/내구도/배터리), 레이서형·균형형·탱커형 라인업~~ — 미채택, 아래만 유효
- 드론 스탯(속도/가속/선회/상승하강 속도, 배터리 소모량)은 현재 [src/main.js](../src/main.js)의 `DRONE_STATS` 상수로 이미 구현되어 있음 — 기체 여러 종 대신 이 수치를 장비로 조정하는 방향

### 3.2 장비 선택
> **결정 (2026-09-13)**: 장비는 **총기(전투용)**, **카메라(FPV용)** 위주로 진행. 카메라 장착 시 조종기 화면([index.html](../index.html)의 `#fpvScreen`, 현재는 미장착 검은 화면)에 실제 드론 시점이 뜨도록 연결. 무기/카메라를 장착·발사하려면 **화면 하단 조종기(RC 도크)에도 대응 버튼을 새로 만들어야 함** (예: 발사 버튼, 카메라 전환 버튼) — 스틱 2개 구성에 버튼 추가.

- ~~슬롯 구성(프로펠러/배터리팩/카메라/보조장치), 스탯 가산·트레이드오프~~ — 미채택
- 확정된 슬롯: **총기**(전투 모드에서 발사체 사용), **카메라**(FPV 화면 연동)
- 구현 시 필요한 것: (1) 장비 장착 UI, (2) 총기 발사 로직 + 조종기 발사 버튼, (3) 카메라 장착 시 드론 시점 렌더링(2번째 카메라)을 `#fpvScreen`에 표시

### 3.3 비행 과제(미션) 설계
- 튜토리얼 → 초급 → 중급 → 고급 순으로 난이도 상승, 각 단계 3~5개 과제
- 과제 유형(안):
  1. **이착륙 연습** — 정해진 착륙 패드에 부드럽게 착지 (급하강 시 감점/실패)
  2. **호버링(정지비행)** — 표시된 구역 안에서 n초간 제자리 유지
  3. **게이트 통과** — 공중에 뜬 링/게이트를 순서대로 통과, 타임어택
  4. **좁은 통로 비행** — 장애물 사이를 충돌 없이 통과 (정밀 조작)
  5. **경로 추적/정찰** — 지정된 경로를 따라 이동하며 목표물 촬영/확인
- 과제마다 별 1~3개 평가(성공/정확도/시간 기준) → 별 개수로 보상(코인) 차등 지급
- 교육적 요소는 4장에서 별도 정리

### 3.4 전투 모드 (최종 단계)
> **결정 (2026-09-13)**: **안 B. 탄막형 대결**로 확정. 멀티플레이는 불필요 — **AI 상대와 1인용**으로 진행 (파이어베이스 실시간 동기화 설계 불필요). 상대 드론 에셋은 `enemy/`, `npc/` 폴더에 이미 확보해둠 (Bot Drone, Stinger Drone, Little Drone GLB).

- ~~안 A. 서바이벌 격투(밀어내기/링아웃)~~ — 미채택
- **채택: 안 B. 탄막형 대결** — 드론에 장착한 총기(3.2)로 소프트한 발사체를 쏘고, 맞으면 체력(배터리?) 감소. 체력 0이 되면 리스폰 또는 라운드 종료
- 상대: **AI 드론 1기 이상** (`enemy`/`npc` 에셋 활용), 실시간 멀티 없음

톤 관련 기존 제안(스포츠 경기 느낌으로, "격추"보다 순화된 표현 사용)은 유효 — 구현 시 참고.

**남은 결정/구현 항목**:
- [ ] 1:1인지, AI 여러 기와 동시에 붙는 난전인지 — 아직 미정
- [x] ~~실시간 멀티플레이 필요 여부~~ → 불필요, AI 상대로 결정
- [ ] 한 판 플레이 시간 (1~2분 내외 추천, 아직 미확정 — 4번 항목과 함께 보류)
- [ ] 발사체 종류/피격 판정/리스폰 규칙 구체화
- [ ] AI 드론의 이동/조준 패턴 설계

## 4. 교육적으로 고려하면 좋을 점 (제안)

- 미션 이름/설명에 실제 드론 조종 개념을 자연스럽게 녹이기 (예: 호버링 과제 설명에 "짐벌/자이로가 균형을 잡아준다" 같은 짧은 설명 카드 추가) — 단순 게임을 넘어 원리 이해로 연결
- 배터리 스탯을 통해 "에너지 관리"라는 실생활 개념 체험 (배터리 다 쓰면 불시착 → 다시 도전)
- 전투 모드에서도 "안전 수칙" 톤을 살짝 넣으면 좋음 — 예: 파괴/격추보다는 "귀환시키기", "밀어내기" 같은 표현으로 순화
- 격투 모드가 지나치게 자극적인 경쟁으로 흐르지 않도록, 승패보다 "정확한 조작"에 점수 가중치를 더 주는 방식도 고려 가능

## 5. 조작 방식 (안)

- **태블릿**: 화면 좌측 가상 조이스틱(상승/하강 + 좌우 회전), 우측 가상 조이스틱(전후/좌우 이동) — 실제 드론 조종기의 스로틀/러더 vs 엘리베이터/에일러론 구조를 단순화
- **PC**: WASD(이동) + Q/E(회전) + Space/Shift(상승/하강) 또는 마우스 시점 연동
- 두 조작 방식 모두 감도(민감도) 조절 옵션 필요 — 초등학생 손가락 터치 오차 고려해 태블릿 쪽 데드존 넉넉하게
- 실물 조종기 스타일 UI(스틱 2개 + 화면)로 이미 구현됨 — **결정 (2026-09-13)**: 3.2 장비(총기/카메라) 도입에 맞춰 이 조종기에 **버튼을 추가**해야 함 (발사 버튼, 카메라 전환 버튼 등). 스틱 배치는 유지하고 버튼만 얹는 방향으로 설계

## 6. 성장/보상 시스템

- 과제 클리어 → 코인 + 부품(장비 뽑기용 재료) 획득
- 코인으로 장비 구매/업그레이드, 부품은 특정 등급 장비 해금에 사용
- 전투 모드는 별도 랭크/포인트 시스템(승/패 기록) — 다만 순위 노출은 교실 분위기 고려해 과열되지 않게 설계 필요

## 7. 기술 스택 제안

- 웹 기반 3D — Three.js (또는 유사 라이브러리) 사용, 3D RPG 데모 프로젝트에서 이미 KayKit 등 무료 3D 에셋을 다뤄보신 경험 활용 가능
- 터치 컨트롤 UI는 오버레이 형태의 HTML/CSS 가상 조이스틱으로 구현
- 실시간 멀티플레이가 필요하면 기존에 쓰시던 파이어베이스 Firestore/Realtime DB로 위치·상태 동기화

## 8. 다음 단계

**결정된 것 (2026-09-13)**:
1. ~~전투 모드 방향(안 A/B)~~ → **안 B. 탄막형 대결**로 확정
2. ~~실시간 멀티플레이 필요 여부~~ → **불필요, AI 상대 1인용**으로 확정
3. ~~드론 종류를 몇 종으로 시작할지~~ → **기체는 1종만 사용**, 다양성은 장비(총기/카메라)로 대체
4. 세션 타임라인(수업 40분 구성) → **지금 단계에서는 정하지 않기로 함** (보류)

**아직 열려있는 것**:
- [ ] 전투 1:1 vs AI 여러 기 동시 난전 여부
- [ ] 발사체 종류, 피격 시 체력 감소량, 리스폰 규칙
- [ ] AI 상대 드론의 이동/조준 패턴 (`enemy`/`npc` 폴더 에셋 활용)
- [ ] 장비(총기/카메라) 장착 UI 설계
- [ ] 카메라 장비 장착 시 FPV 화면(`#fpvScreen`) 실제 렌더링 연결
- [ ] 조종기 UI에 무기 발사 / 카메라 전환 버튼 추가

**구현 현황 요약** (참고용):
- 완료: 비행 훈련장(링 통과 + 착륙 미션), 도심 자유비행 + 배송 미션, 충전 스테이션까지 구현·배포됨
- 다음으로 이어갈 부분: 위 "아직 열려있는 것" 항목들 — 총기/카메라 장비 시스템부터 시작하는 게 순서상 자연스러움 (전투 모드가 이 장비들에 의존하므로)

---
*이 문서는 초안이며, 결정 사항을 반영해 계속 업데이트해나갈 예정입니다.*
